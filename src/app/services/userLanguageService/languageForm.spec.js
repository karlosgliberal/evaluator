//import languageFormServices from './languageForm.services';
//import App from '../../app';
//
//describe('language form select service', () => {
//
//  var scope, languageFormService, translateStub, sandbox, stubs;
//
//  before(() => {
//    sandbox = sinon.sandbox.create();
//  });
//
//  beforeEach(angular.mock.module(languageFormService.name, ($provide) => {
//    translateStub = {instant: sinon.stub()};
//    $provide.value('$translate', translateStub);
//  }));
//
//  beforeEach(angular.mock.module(languageFormService.name));
//
//  beforeEach(inject((_languageFormService_, $rootScope, $compile)=> {
//    scope = $rootScope.$new();
//    scope.model = {};
//
//    stubs = {
//      $state: {
//        go: sandbox.stub()
//      }
//    };
//      //languageFormService = {languageFormService : _languageFormService_, createFieldGroup: sinon.spy()};
//    languageFormService = _languageFormService_;
//  }));

  //it('should create form', () => {
    //var transStub = sinon.stub($translate, 'use');
    //transStub.withArgs(fieldList).returns('es');
    //
    //expect(languageFormService.generateLanguageForm()).to.have.deep.property('[0].key', 'language');
//  });
//});
