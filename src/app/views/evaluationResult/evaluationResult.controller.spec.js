import evaluationResultControllers from './evaluationResult.controllers';

var controller, scope, stateSpy;

describe('Idioma controller', () => {
  //beforeEach(inject(($controller) => {
  //  scope = $rootScope.$new();
  //  stateSpy = {
  //    $state: {
  //      go: sinon.spy()
  //    }
  //  };
  //
  //  controller = $controller('IdiomaController', {
  //    languageFormService: formServiceSpy,
  //    state: $state
  //  });
  //}));
});
